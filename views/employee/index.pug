extends ../layout

block content
 div(claass="container")
  h1= title
  nav
    ul
      li
        a(href="employee/news/add") Dodaj
  div(class="form")      
   table
    tr
      th id
      th tytuł
      th data dodania
      th akcja
    each item, index in data 
      tr
        td=index
        td=item.title
        td=item.created
        td  
          a(href=`/employee/news/delete/${item._id}`) Usuń